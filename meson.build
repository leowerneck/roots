project( 'Roots',
         'c',
         version : '0.5.0',
         license : 'BSD-2-Clause license',
         default_options : ['buildtype=release',
                            'c_args=-march=native',
                            'c_std=gnu99'])

subdir('roots')
subdir('test')
